"""입찰메이트 RFP RAG.

`src/` 안에서는 평평하게 import 한다. 노트북과 `scripts/` 는 맨 앞에서
`src/` 와 프로젝트 루트를 `sys.path` 에 넣고 시작한다.

    from config import settings
    from preprocessing import load_documents
"""

__version__ = "0.1.0"
